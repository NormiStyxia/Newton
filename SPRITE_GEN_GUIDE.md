# 序列帧 MCP 使用指南

序列帧 MCP（`sprite-gen`）用于根据一张参考图生成角色、怪物、道具或特效的透明背景序列帧动画。

## 1. 适用场景

支持两类精灵：

| 类型 | 参数 | 适用对象 |
|---|---|---|
| 角色精灵 | `character` | 人物、动物、怪物、机器人等 |
| 通用精灵 | `generic` | 武器、道具、机关、载具、特效物体等 |

适合生成：

- `idle`、`walk`、`run` 等循环动画
- `attack`、`hurt`、`death` 等单次动画
- `jump`、`fall`、`land` 等空中动画
- `cast`、`dash`、`fly` 等特殊动画
- 自定义物体运动或特效动画

## 2. 最简单的使用方式

准备一张角色或物体参考图，然后直接说明需求：

```text
请用这张图生成角色序列帧。

精灵名称：火焰骑士
类型：character
角色高度：128px
视角：伪侧面，面向画面右侧
风格：保持原图
帧模式：K

动画：
1. idle，循环，1.5秒
2. run，循环，0.8秒
3. attack，单次，0.7秒
4. hurt，单次，0.4秒
5. death，单次，1.5秒
```

如果不确定参数，也可以直接说：

```text
用默认配置，角色视角选择第4项，生成 idle、run 和 attack。
```

## 3. 标准工作流程

```text
提供参考图
  ↓
选择精灵类型
  ↓
处理角色视角
  ↓
设置输出尺寸和帧模式
  ↓
确认动画列表
  ↓
确认动作姿态
  ↓
逐条生成动画
  ↓
检查并重做不合格动画
  ↓
导出序列帧产物
```

生成过程中会在关键阶段暂停，请确认后再继续。不要在视角或动作方案明显不正确时直接推进到最终生成。

## 4. 参考图要求

### 推荐参考图

- 主体完整，不被画面边缘裁切
- 背景简单，与主体轮廓有明显区分
- 角色四肢清晰
- 服装和装备结构明确
- 不带复杂文字、水印或 UI
- 分辨率足以看清人物细节
- 横版角色建议面向画面右侧

### 不推荐参考图

- 多个角色同时出现
- 手脚被遮挡
- 动作透视过于夸张
- 背景与主体颜色接近
- 主体只占图片很小区域
- 图片严重模糊或压缩
- 角色身上带大量复杂粒子特效

### 输入形式

支持：

- 工作区图片路径
- 可访问的图片 URL
- 对话中提供的图片

## 5. 角色视角设置

`character` 类型支持以下处理方式：

| 选项 | 名称 | 说明 |
|---:|---|---|
| 1 | `keep` | 保持原图，不调整 |
| 2 | `flip` | 水平翻转，使角色面向右侧 |
| 3 | `profile` | 转为标准侧面中性站姿 |
| 4 | `cheated` | 转为伪侧面，略微露出躯干和双腿 |
| 5 | `custom` | 按自定义要求调整 |

横版动作游戏通常推荐选项 4：伪侧面、面向画面右侧、中性站姿。伪侧面比严格侧面更容易看清前后腿、双手动作、服装和武器挥动方向。

自定义示例：

```text
保持三分之二侧面，面向右侧，双手自然下垂。
保持法杖在右手，但移除法杖周围的粒子特效。
```

## 6. 输出尺寸

### `target_height`

默认值：

```text
256px
```

它表示精灵主体的实际内容高度，不包含外围透明边距。例如设置 `target_height = 128`，表示角色从头顶到脚底约为 128 像素；最终单帧画布可能更大，以容纳奔跑、跳跃、挥剑等动作。

推荐尺寸：

| 使用场景 | 建议高度 |
|---|---:|
| 小型像素角色 | 32～64px |
| 普通像素角色 | 64～128px |
| 高清横版角色 | 128～256px |
| 大型怪物或 Boss | 256～512px |
| 高细节角色 | 512px 以上 |

建议按游戏中的预期显示尺寸生成，不要过度依赖运行时缩放。

## 7. 画布占比

### `content_ratio`

默认值：

```text
0.6
```

有效范围：

```text
0.01～1.0
```

表示主体占准备画布的比例：

| 设置 | 效果 | 适用情况 |
|---:|---|---|
| `0.4～0.5` | 四周空间较多 | 跳跃、翻滚、大范围挥剑 |
| `0.55～0.65` | 空间和细节平衡 | 普通角色，推荐 |
| `0.7～0.8` | 主体较大，细节更多 | 小幅度 Idle、道具动画 |
| 接近 `1.0` | 几乎无动作余量 | 通常不推荐 |

如果动作经常出框，降低该值；如果主体太小或细节不足，提高该值。

### 画布偏移

高级选项：

```text
offset_ratio_x
offset_ratio_y
```

含义：

- `offset_ratio_x > 0`：主体向右移动
- `offset_ratio_x < 0`：主体向左移动
- `offset_ratio_y > 0`：主体向下移动
- `offset_ratio_y < 0`：主体向上移动

角色模式默认垂直偏移约为 `offset_ratio_y = 0.1`，使角色脚底更靠近画布下方，为头顶和跳跃动作预留空间。

## 8. 帧模式

### F：全帧模式

```text
frame_mode = F
```

按照设置的 FPS 导出完整帧，理论帧数为：

```text
帧数 ≈ FPS × 动画时长
```

例如 `24 FPS × 1.5 秒 ≈ 36 帧`。

适合：

- 高清角色
- 平滑奔跑
- 流畅攻击
- 连续特效
- 需要保留完整动作过程的动画

缺点是帧数和文件体积较大，可能包含变化很小的相邻帧。

### K：关键姿态模式

```text
frame_mode = K
```

自动提取关键姿态，减少重复或相似帧。适合：

- 像素风
- 逐帧感动画
- 移动端小游戏
- 希望减少资源体积
- 不要求视频级流畅度的动画

最终帧数不是固定值，取决于动作变化幅度。复杂动作通常保留更多关键帧，简单 Idle 通常保留较少帧。

## 9. 帧率和时长

默认设置：

```text
fps = 24
video_duration = 5秒
```

默认配置适合生成阶段，但 `24 FPS × 5 秒` 可能产生约 120 帧，对普通角色动作通常偏多。建议根据动作设置更短时长。

| 动画 | 时长 | FPS | 播放方式 |
|---|---:|---:|---|
| Idle | 1.5～3秒 | 8～24 | 循环 |
| Walk | 0.8～1.2秒 | 8～16 | 循环 |
| Run | 0.5～1秒 | 12～24 | 循环 |
| Jump | 0.4～0.8秒 | 12～24 | 单次 |
| Fall | 0.5～1秒 | 8～16 | 循环或保持 |
| Land | 0.2～0.5秒 | 12～24 | 单次 |
| Attack | 0.4～1.5秒 | 12～24 | 单次 |
| Hurt | 0.2～0.6秒 | 8～16 | 单次 |
| Death | 1～3秒 | 12～24 | 单次 |
| Cast | 0.8～2秒 | 12～24 | 单次 |
| Dash | 0.3～0.7秒 | 12～24 | 单次 |

像素风建议：

```text
target_height = 64 或 128
frame_mode = K
fps = 8～12
```

高清角色建议：

```text
target_height = 256
frame_mode = F
fps = 18～24
```

## 10. 动画播放类型

循环播放适合 `idle`、`walk`、`run`、`fly`、`hover` 和循环特效。循环动作应尽量让首尾姿态一致。

单次播放适合 `attack`、`hurt`、`death`、`jump`、`land`、`dash`、开门和爆炸。单次动作应明确起始姿态、动作高潮和结束姿态。

## 11. 动作描述写法

动作要求越具体，结果越稳定。

不推荐：

```text
攻击
```

推荐：

```text
角色向前快速横斩一次，双脚保持在地面，身体不发生位移。
动作从中性站姿开始，并在挥剑结束后回到中性站姿。
```

循环奔跑：

```text
角色原地向右奔跑，不发生水平位移。
保持完整侧面运动，双腿交替，手臂自然摆动。
动作首尾连续，可无缝循环。
```

跳跃：

```text
角色从地面起跳，身体向上抬升，双腿收起。
保持面向右侧，不翻转，不改变角色设计。
这是单次动作，不需要循环。
```

近战攻击：

```text
角色面向右侧挥剑横斩一次。
武器从身后蓄力，经身体前方快速划过。
脚底位置保持稳定，不向前移动，不增加额外武器。
```

## 12. 地面与空中姿态

角色模式支持：

```text
ready_pose = ground
ready_pose = aerial
```

- `ground`：站立角色、地面怪物、步行和奔跑角色
- `aerial`：飞行角色、悬浮角色、飞鸟和空中怪物

如果参考图本身就是飞行或悬浮姿态，应选择 `aerial`，避免工具误判角色缺少地面站姿。

## 13. 生成质量和分辨率

### 质量档位

```text
video_generation_tier = fast
video_generation_tier = default
```

| 档位 | 说明 |
|---|---|
| `fast` | 优先生成速度，默认 |
| `default` | 优先质量和稳定性 |

测试动作方案可使用 `fast`；最终角色、Boss、主角或复杂装备建议使用 `default`。

### 中间动画分辨率

支持：

```text
480
720
1080
null
```

`null` 表示自动推导，当前默认通常使用 720。该分辨率是动画生成阶段的期望分辨率，不是最终单帧 PNG 尺寸；最终主体尺寸由 `target_height` 控制。

## 14. 推荐配置模板

### 像素风横版角色

```text
类型：character
视角：伪侧面，面向右侧
主体高度：64px
画布占比：0.55
帧模式：K
帧率：10 FPS
质量档位：default

动画：
- idle：1.6秒，循环
- walk：0.8秒，循环
- run：0.6秒，循环
- jump：0.5秒，单次
- attack：0.7秒，单次
- hurt：0.3秒，单次
- death：1.2秒，单次
```

### 高清横版动作角色

```text
类型：character
视角：伪侧面，面向右侧
主体高度：256px
画布占比：0.5
帧模式：F
帧率：24 FPS
质量档位：default
中间分辨率：1080

动画：
- idle：2秒，循环
- run：0.8秒，循环
- jump：0.6秒，单次
- attack_1：0.7秒，单次
- attack_2：0.9秒，单次
- hurt：0.4秒，单次
- death：1.8秒，单次
```

### 道具或机关

```text
类型：generic
保持原图视角
主体高度：128px
画布占比：0.65
帧模式：K
帧率：12 FPS

动画：
- activate：0.8秒，单次
- active：1.5秒，循环
- deactivate：0.8秒，单次
```

## 15. 生成后的修改

### 新增动画

```text
给这个精灵新增一个 dash 动画：
时长0.5秒，单次播放，角色快速向右突进。
```

### 修改动画

可调整 `action`、`duration`、`user_prompt` 和 `play_type`：

```text
把 attack 动画改成0.6秒，单次播放。
挥剑速度更快，但不要改变脚底位置。
```

### 修改精灵级参数

可修改 `target_height` 和 `frame_mode`：

```text
把输出主体高度改成128px，并切换为K关键姿态模式。
```

工具会先展示修改影响，确认后再应用。

### 重做动画

```text
重做 run 动画，保持角色原地，不要让角色在画面中向前移动。
```

也可以只重做指定动画，无需重新生成整个精灵。

### 回退阶段

支持回退到：

- `ready`：参考图准备阶段
- `viewpoint`：视角调整阶段
- `plan`：动画规划阶段
- `pose`：姿态处理阶段

## 16. MCP 指令参考

| 指令 | 用途 |
|---|---|
| `next` | 确认当前阶段并继续 |
| `goto` | 回退到指定阶段或动画 |
| `redo` | 重做当前步骤或指定动画 |
| `add` | 新增动画 |
| `update` | 修改精灵或动画参数 |
| `describe` | 查看当前全部配置 |
| `help` | 查看指令帮助 |

通常不需要手动编写底层调用，直接用自然语言说明即可。

示例：

```text
展开更多选项。
```

```text
describe --sprite-name <精灵名称>
redo --sprite-name <精灵名称> --anim-id <动画ID>
goto --sprite-name <精灵名称> --stage plan
```

## 17. 常见问题

### 动作超出画布

1. 降低 `content_ratio`
2. 调整画布偏移
3. 减小动作幅度
4. 在动作描述中要求“保持在画布范围内”

推荐尝试 `content_ratio = 0.45～0.55`。

### 角色在画面中向前移动

在动作描述中增加：

```text
角色原地运动，脚底中心位置保持固定，不产生水平位移。
```

### 跑步首尾不连续

增加：

```text
动作必须无缝循环，最后一帧自然衔接第一帧。
```

### 武器形状变化

增加：

```text
严格保持参考图中的武器设计、长度、颜色和持握方式。
不新增武器，不改变武器结构。
```

### 相邻帧过多

- 改用 `K` 模式
- 降低 FPS
- 缩短动画时长

### 动画不够流畅

- 改用 `F` 模式
- 提高 FPS
- 使用 `default` 质量档位
- 提高中间动画分辨率

### 角色细节模糊

- 提高 `target_height`
- 提高 `content_ratio`
- 使用更清晰的参考图
- 将生成档位改为 `default`
- 将中间动画分辨率提高到 720 或 1080

## 18. 完整请求范例

```text
请根据我提供的图片生成一套横版动作角色序列帧。

精灵名称：霜刃女武士
精灵类型：character

参考图处理：
- 使用伪侧面视角
- 面向画面右侧
- 保持角色服装、发型、配色和长剑设计
- 调整为中性站姿
- 不保留背景

输出配置：
- 主体高度：128px
- 画布占比：0.5
- 帧模式：K
- 帧率：12 FPS
- 质量档位：default
- 美术风格：保持原图卡通赛璐璐风格

动画：
1. idle
   - 时长：2秒
   - 循环
   - 呼吸起伏和头发轻微摆动

2. run
   - 时长：0.8秒
   - 循环
   - 原地向右奔跑，不产生水平位移
   - 首尾无缝衔接

3. attack
   - 时长：0.7秒
   - 单次
   - 长剑从身后向前横斩
   - 双脚保持在地面
   - 攻击结束后恢复中性站姿

4. hurt
   - 时长：0.4秒
   - 单次
   - 上半身后仰，不倒地

5. death
   - 时长：1.5秒
   - 单次
   - 角色向后倒地，结束后保持倒地姿态

先展示视角调整结果和动画规划，等我确认后再继续生成。
```

完整请求应尽量明确主体尺寸、视角、是否原地运动、循环方式、动作时长，以及需要保持不变的角色特征。
